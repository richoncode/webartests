#pragma once
// Bumped automatically by build.sh on every compile. Shown on every screen so a
// glance at the device settles which build is actually running — asking for
// feedback without that is how a fix and the report on it drift apart.
#define FW_BUILD 2
#define FW_BUILD_TIME __TIME__
