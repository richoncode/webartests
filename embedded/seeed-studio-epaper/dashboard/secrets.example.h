#pragma once
// Copy to secrets.h and fill in. secrets.h is gitignored.
// The ESP32-S3 has no 5 GHz radio — the SSID must be a 2.4 GHz network.
#define WIFI_SSID     "your-2.4GHz-ssid"
#define WIFI_PASSWORD "your-password"
